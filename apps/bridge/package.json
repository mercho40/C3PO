{
  "name": "@repo/bridge",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "uv run --project . python -m bridge.main",
    "mcp": "uv run --project . python -m bridge.mcp_server",
    "build": "uv sync",
    "check-types": "echo 'mypy not yet configured'",
    "test": "uv run --project . pytest"
  }
}
